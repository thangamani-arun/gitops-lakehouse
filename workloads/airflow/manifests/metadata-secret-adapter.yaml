# CNPG's "airflow-pg-app" secret already carries a ready-made `uri` key
# (postgresql://user:pass@host:port/db); the Airflow chart just wants that same string under
# a `connection` key. This Job is the adapter, so we don't have to hand-manage the password
# anywhere. Runs before the Helm-templated Airflow resources (sync-wave -1 vs their default 0)
# and must reach Complete before ArgoCD proceeds.
#
# Deliberately NOT an ArgoCD PreSync hook: hooks run in a separate phase strictly *before* any
# normal (non-hook) resource syncs, including this Job's own ServiceAccount below -- that caused
# "serviceaccount not found" every time. Plain sync-wave ordering (this file's wave -1 vs the
# Helm chart's default wave 0) already gives the same "SA/Role/RoleBinding/Job first, wait for
# the Job to succeed, then the chart" behavior without the phase mismatch.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: airflow-metadata-secret-adapter
  namespace: lakehouse-orchestration
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: airflow-metadata-secret-adapter
  namespace: lakehouse-orchestration
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: airflow-metadata-secret-adapter
  namespace: lakehouse-orchestration
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
subjects:
  - kind: ServiceAccount
    name: airflow-metadata-secret-adapter
roleRef:
  kind: Role
  name: airflow-metadata-secret-adapter
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: batch/v1
kind: Job
metadata:
  name: airflow-metadata-secret-adapter
  namespace: lakehouse-orchestration
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  backoffLimit: 6
  template:
    spec:
      serviceAccountName: airflow-metadata-secret-adapter
      restartPolicy: OnFailure
      containers:
        - name: adapter
          image: bitnamilegacy/kubectl:1.31.4-debian-12-r1
          command:
            - /bin/bash
            - -c
            - |
              set -euo pipefail
              URI=$(kubectl get secret airflow-pg-app -n lakehouse-orchestration -o jsonpath='{.data.uri}' | base64 -d)
              kubectl create secret generic airflow-metadata-connection \
                -n lakehouse-orchestration \
                --from-literal=connection="${URI}" \
                --dry-run=client -o yaml | kubectl apply -f -
